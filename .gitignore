# Dependencies
node_modules/
# also ignore a root node_modules symlink (the `/` form matches a file/symlink,
# not just a directory) so it can never be force-added again — see SPLIT-276
/node_modules

# Next.js
.next/
out/

# Environment variables
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# Build output
dist/
coverage/

# OS
.DS_Store

# IDE
.vscode/
.idea/

# Vercel
.vercel/

# TypeScript incremental build cache (machine-specific)
*.tsbuildinfo
