# ── Auth ──────────────────────────────────────────────────────────────────────
# Operator key for the MCP endpoint (deny-by-default — required, or every request
# is refused). Callers present it as `x-api-key`, or send a user backend JWT as a
# Bearer token (issued by the backend's POST /api/v1/users/login).
MCP_API_KEY=your_operator_key

# RECOMMENDED: the backend's JWT_SECRET. Bearer identity is verified either way
# — never decoded-and-trusted — but this is the fast path: with it set, the MCP
# proves the HS256 signature + expiry in-process. WITHOUT it the MCP must ask the
# backend who the caller is (GET /api/v1/users/profile) on every cache miss,
# which costs one upstream round-trip per caller per minute and makes auth
# dependent on backend availability (it fails CLOSED). Set it to the exact same
# value as the backend's JWT_SECRET, or bearer auth gets slower, not weaker.
MCP_BACKEND_JWT_SECRET=your_backend_jwt_secret

# ── Backend REST API (the single source of truth for all data) ─────────────────
# All booking/messaging/experience/listing/pricing tools call the backend REST
# API forwarding the caller's JWT. Defaults to production if unset.
# BACKEND_API_URL=https://splitmygear-backend.vercel.app/api/v1

# ── No database / payment credentials ─────────────────────────────────────────
# The MCP holds NO Supabase or Stripe client — it reads/writes exclusively
# through the backend REST API. SUPABASE_*, SUPABASE_SERVICE_ROLE_KEY and
# STRIPE_SECRET_KEY are no longer referenced by any code (remove + rotate any
# that were provisioned).

# ── Rate limiting ───────────────────────────────────────────────────────────────
# Tier: internal (100/min), beta (50/min), public (20/min)
MCP_RATE_LIMIT_TIER=public
